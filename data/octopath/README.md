These start as empty catalogs so parsing never fails - items/monsters just
show up as "Unknown item/monster <id>" instead of their real names.

For real names, download the full catalogs from the upstream project and
drop them in here with the same filenames:
  https://github.com/AppleBoiy/octopath-save-editor/tree/main/static
  (items.json, items-ot2.json, monsters-ot1.json)
